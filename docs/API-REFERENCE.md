# Endpoint reference

CGPanel 0.5.0. Generated from `scripts/build_api_docs.py`; do not edit this file directly.

Use the [API guide](https://github.com/ItzGlace/CGPanel/blob/main/docs/API.md) for authentication and workflows. All paths below include their API prefix. An administrator session or token is required where stated; other management endpoints also accept authorized tenant sessions. Cookie-based non-GET calls require CSRF. Read-only bearer tokens cannot call POST, even for read-like actions.

## Endpoint index

| Method | Path | Purpose |
| --- | --- | --- |
| POST | `/api/v4/files/{id}` | Workspace file operations |
| GET | `/api/v4/runtimes` | Approved runtime versions |
| GET | `/api/v4/runtimes/{id}` | Current application runtime |
| GET | `/api/v4/ide/{id}` | Workspace IDE status |
| POST | `/api/v4/ide/{id}/password` | Read workspace IDE password |
| GET | `/api/v4/updates` | Panel update status |
| POST | `/api/v4/updates` | Configure or run panel updates |
| GET | `/healthz` | HTTP process health |
| POST | `/api/login` | Sign in |
| GET | `/api/me` | Current identity |
| POST | `/api/logout` | End browser session |
| POST | `/api/password` | Change own password |
| GET | `/api/admin/system` | Administrator system overview |
| GET | `/api/admin/audit` | Paginated audit events |
| GET | `/api/admin/tokens` | List own API token metadata |
| POST | `/api/admin/tokens` | Create administrator API token |
| DELETE | `/api/admin/tokens/{id}` | Revoke own API token |
| GET | `/api/admin/openapi.json` | Download OpenAPI contract |
| GET | `/api/docs` | List accessible documentation |
| GET | `/api/docs/{slug}` | Read bundled guide |
| GET | `/api/overview` | Workspace overview |
| GET | `/api/users` | List accounts |
| POST | `/api/users` | Create tenant account |
| POST | `/api/users/{id}` | Replace tenant access settings |
| GET | `/api/resources/{kind}` | List resources |
| POST | `/api/resources/{kind}` | Create managed resource |
| DELETE | `/api/resource/{id}` | Delete a resource |
| POST | `/api/resource/{id}/{action}` | Run resource action |
| GET | `/api/v2/integrations` | List redacted integrations |
| POST | `/api/v2/integrations` | Create or edit integration |
| DELETE | `/api/v2/integrations/{id}` | Delete integration |
| GET | `/api/v2/jobs` | List background jobs |
| POST | `/api/v2/jobs` | Submit background operation |
| POST | `/api/v2/jobs/{id}/retry` | Retry failed or interrupted job |
| POST | `/api/v2/cron-preview` | Preview next five cron occurrences |
| GET | `/api/v2/backup-plans` | List automatic backup plans |
| POST | `/api/v2/backup-plans` | Create automatic backup plan |
| DELETE | `/api/v2/backup-plans/{id}` | Remove automatic backup plan |
| GET | `/api/v2/backups` | List full .cgp archives |
| DELETE | `/api/v2/backups/{id}` | Delete local full archive |
| GET | `/api/v2/backups/{id}/download` | Download .cgp archive |
| GET | `/api/v2/monitor/{id}` | Read website monitoring |
| POST | `/api/v2/monitor/{id}` | Replace monitoring configuration |
| GET | `/api/v2/analytics/{id}` | Read website analytics and heatmap |
| GET | `/api/v2/domains/{id}/tls` | Certificate and renewal status |
| GET | `/api/v2/domains/{id}/zone` | Download BIND DNS zone |
| POST | `/api/v2/domains/{id}/cdn` | Set trusted CDN mode |
| GET | `/api/v2/egress/{id}` | Read application proxy policy |
| POST | `/api/v5/apps/{id}/console` | Stream application command output |
| POST | `/api/v5/network-range` | Preview IP and CIDR ranges |
| POST | `/api/v5/apps/{id}/git` | Workspace Git tools |
| GET | `/api/v5/apps/{id}/services` | Transfer service settings |
| POST | `/api/v5/apps/{id}/services` | Configure SFTP and FTPS |
| GET | `/api/v5/users/{id}/budget` | Read account resource budget |
| POST | `/api/v5/users/{id}/budget` | Set account resource budget |
| POST | `/api/v5/apps/{id}/limits` | Allocate application resources |
| POST | `/api/v5/apps/{id}/backup-import` | Upload a cgp archive |
| GET | `/api/v5/protection/{id}` | Website protection settings |
| POST | `/api/v5/protection/{id}` | Configure website protections |
| GET | `/api/v5/protection/{id}/traffic` | Recent traffic summary |
| GET | `/api/v5/mfa` | Own MFA status |
| POST | `/api/v5/mfa/enroll` | Begin authenticator enrollment |
| POST | `/api/v5/mfa/confirm` | Confirm authenticator enrollment |
| POST | `/api/v5/mfa/disable` | Disable own MFA |
| GET | `/api/v5/mail` | Mail hosting status |
| POST | `/api/v5/mail/server` | Configure mail server |
| POST | `/api/v5/mail/domains/{id}` | Enable domain mail |
| POST | `/api/v5/mail/mailboxes` | Create or update mailbox |

## POST /api/v4/files/{id}

Workspace file operations. Owned application only. See the v0.4 guide for bounded uploads/downloads, revision-checked edits and file operations. Works with stopped applications.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "operation": "list",
  "path": "",
  "offset": 0
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v4/runtimes

Approved runtime versions. Version tracks available for per-application runtime jobs.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v4/runtimes/{id}

Current application runtime. Owned application runtime, image and command.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v4/ide/{id}

Workspace IDE status. Owned application code-server installation, state and separate-origin URL.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v4/ide/{id}/password

Read workspace IDE password. Session and CSRF only. Owned application must have its IDE enabled. Passwords never appear in job results.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v4/updates

Panel update status. Administrator only. Installed version, settings and latest updater state.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v4/updates

Configure or run panel updates. Administrator only. settings changes enabled; check/apply queue a root-owned system service.

Request schema: `object` in the OpenAPI contract.

```json
{
  "action": "settings",
  "enabled": false
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /healthz

HTTP process health. Does not check broker, DNS, containers or databases.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/login

Sign in. Sets cg_session cookie for eight hours and returns csrf. Login throttling applies.

Request schema: `Login` in the OpenAPI contract.

```json
{
  "username": "admin",
  "password": "YOUR_PASSWORD"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/me

Current identity. Returns id, username, role and csrf. Bearer csrf is empty.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/Identity"
    }
  }
}
```

## POST /api/logout

End browser session. Session and CSRF only. Does not revoke API tokens.

Request schema: `object` in the OpenAPI contract.

```json
{}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/password

Change own password. Session and CSRF only. Requires current password; new password 14–256 characters. Revokes all own sessions and API tokens.

Request schema: `object` in the OpenAPI contract.

```json
{
  "current": "CURRENT_PASSWORD",
  "password": "NEW_PASSWORD"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/admin/system

Administrator system overview. Administrator only. Version, resource counts, recent events and broker host health.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/System"
    }
  }
}
```

## GET /api/admin/audit

Paginated audit events. Administrator only. Events in descending ID order. Pass next_before as before until null. Includes token IDs for bearer mutations.

- `limit` (query): 1–200, defaults to 50
- `before` (query): Exclusive positive event ID cursor


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "properties": {
        "events": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "next_before": {
          "type": "integer",
          "nullable": true
        }
      }
    }
  }
}
```

## GET /api/admin/tokens

List own API token metadata. Administrator session only. Latest 200 records; no secret or hash. Expired and revoked records remain visible.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/TokenMetadata"
      }
    }
  }
}
```

## POST /api/admin/tokens

Create administrator API token. Administrator session and CSRF only. Secret shown once. At most 20 active tokens, 1–90 days; account and token CIDRs both apply. Defaults: read scope, 30 days, no extra IP filter.

Request schema: `TokenCreate` in the OpenAPI contract.

```json
{
  "name": "reporting",
  "scope": "read",
  "expires_days": 30,
  "allowed_ips": [
    "203.0.113.10/32"
  ]
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/TokenSecret"
    }
  }
}
```

## DELETE /api/admin/tokens/{id}

Revoke own API token. Administrator session and CSRF only. Idempotent for an existing own token. Does not cancel in-flight requests or queued jobs.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/admin/openapi.json

Download OpenAPI contract. Administrator only. Same contract checked into docs/openapi.json.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/docs

List accessible documentation. Tenant guides for all signed-in users; additional API/operations guides for administrators.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "slug": {
            "type": "string"
          },
          "title": {
            "type": "string"
          }
        }
      }
    }
  }
}
```

## GET /api/docs/{slug}

Read bundled guide. Known slugs: getting-started, features; admin only: operations, api, reference. Returns sanitized HTML and original Markdown.

- `slug` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "properties": {
        "slug": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "html": {
          "type": "string"
        },
        "markdown": {
          "type": "string"
        }
      }
    }
  }
}
```

## GET /api/overview

Workspace overview. Own counts/events for tenants; all counts/events and host health for administrators.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/System"
    }
  }
}
```

## GET /api/users

List accounts. Administrator only. Includes disabled accounts; no password hashes.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/User"
      }
    }
  }
}
```

## POST /api/users

Create tenant account. Administrator only. Always creates role user. quota defaults to 10 and is clamped 1–100 per resource kind. allowed_ips is up to 32 CIDRs.

Request schema: `UserCreate` in the OpenAPI contract.

```json
{
  "username": "site_owner",
  "password": "REPLACE_WITH_PRIVATE_PASSWORD",
  "quota": 10,
  "allowed_ips": []
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "username": {
          "type": "string"
        }
      }
    }
  }
}
```

## POST /api/users/{id}

Replace tenant access settings. Administrator only, cannot change administrator accounts. Send enabled/quota/allowed_ips together; omitted values reset to true/10/[]. Optional nonempty password resets tenant password. Revokes sessions. Does not stop workloads.

- `id` (path): Opaque path value.

Request schema: `UserUpdate` in the OpenAPI contract.

```json
{
  "enabled": true,
  "quota": 20,
  "allowed_ips": [
    "203.0.113.0/24"
  ]
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/resources/{kind}

List resources. Kinds: apps, domains, databases, dns, schedules, backups (legacy), blocks. Administrator sees all owners; tenant sees own. Passwords and app environment are not returned.

- `kind` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/Resource"
      }
    }
  }
}
```

## POST /api/resources/{kind}

Create managed resource. Use the request schema matching kind. Optional owner selects an active tenant for administrators; otherwise caller. Links must have same owner. New root domains and IP blocks require admin. See resource examples below.

- `kind` (path): Opaque path value.

Request schema: `ResourceCreate` in the OpenAPI contract.

```json
{
  "owner": "TENANT_ID",
  "name": "website",
  "runtime": "php",
  "mode": "web",
  "command": "",
  "env": {}
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/ResourceCreated"
    }
  }
}
```

## DELETE /api/resource/{id}

Delete a resource. Owner or admin; blocks require admin. Remove linked resources first. Deleting an app retains workspace files; database deletion removes its data. Inspect dependencies and backups first.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/resource/{id}/{action}

Run resource action. Apps: start, stop, restart, logs, inspect, terminal, files, read, write. Databases: access. Schedules: status. Legacy workspace backups: restore. Domains: tls (legacy synchronous path; use a TLS job). All use POST, including reads, so read-only bearer tokens cannot call them. See action payload table below.

- `id` (path): Opaque path value.
- `action` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "command": "id"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/integrations

List redacted integrations. Own account unless administrator supplies owner. Secrets omitted; secret-set metadata is returned.

- `owner` (query): Administrator only: tenant ID. Omit to use the authenticated caller; not an all-tenant query.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      }
    }
  }
}
```

## POST /api/v2/integrations

Create or edit integration. Supply id to edit. Creation requires type-specific credentials. Blank secret fields preserve existing values. At most 40 per owner. Referenced/locked proxies cannot be freely changed by tenants.

Request schema: `IntegrationSave` in the OpenAPI contract.

```json
{
  "owner": "TENANT_ID",
  "name": "alerts",
  "type": "telegram",
  "token": "BOT_TOKEN",
  "chat_id": "CHAT_ID",
  "proxy_id": ""
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## DELETE /api/v2/integrations/{id}

Delete integration. Own account unless owner supplied by admin. Remove dependent monitor/backup/proxy references first.

- `id` (path): Opaque path value.
- `owner` (query): Administrator only: tenant ID. Omit to use the authenticated caller; not an all-tenant query.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/jobs

List background jobs. Latest 100 accessible jobs, newest first. Admin sees all; tenant sees own. There is no individual-job GET endpoint.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/Job"
      }
    }
  }
}
```

## POST /api/v2/jobs

Submit background operation. Flat kind/target payload. Supported types and examples below. No idempotency keys. At most 10 pending ordinary jobs per account. owner is needed for admin integration_test/backup_deliver on tenant objects; resource jobs infer it. panel_tls requires admin.

Request schema: `JobSubmission` in the OpenAPI contract.

```json
{
  "kind": "full_backup",
  "target": "APP_ID",
  "database_ids": [
    "DATABASE_ID"
  ],
  "destinations": [],
  "quiesce": true
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/Queued"
    }
  }
}
```

## POST /api/v2/jobs/{id}/retry

Retry failed or interrupted job. Returns a new job ID. restore_full, tls, panel_tls and egress cannot use automatic retry; review and submit a fresh request. Original permissions/ownership apply.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "$ref": "#/components/schemas/Queued"
    }
  }
}
```

## POST /api/v2/cron-preview

Preview next five cron occurrences. Five numeric fields or hourly/daily/weekly aliases. Optional IANA timezone, UTC by default. Returns Unix seconds. POST requires admin-scope token or session CSRF.

Request schema: `Cron` in the OpenAPI contract.

```json
{
  "schedule": "0 3 * * *",
  "timezone": "Asia/Tehran"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "properties": {
        "next": {
          "type": "array",
          "items": {
            "type": "integer"
          }
        }
      }
    }
  }
}
```

## GET /api/v2/backup-plans

List automatic backup plans. Tenant sees own; admin sees all. Includes config, enabled, next_run and last_job.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      }
    }
  }
}
```

## POST /api/v2/backup-plans

Create automatic backup plan. Owner inferred from app. At most 20 per owner. retention defaults 7, clamped 1–30; applies locally after successful delivery. No update endpoint: remove and recreate a plan.

Request schema: `BackupPlan` in the OpenAPI contract.

```json
{
  "app_id": "APP_ID",
  "database_ids": [
    "DATABASE_ID"
  ],
  "destinations": [
    "STORAGE_ID"
  ],
  "schedule": "0 3 * * *",
  "timezone": "UTC",
  "quiesce": true,
  "retention": 7,
  "enabled": true
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## DELETE /api/v2/backup-plans/{id}

Remove automatic backup plan. Removes an accessible schedule, not its existing archives.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/backups

List full .cgp archives. Account scoped. Unlike legacy resources/backups, these contain selected SQL and configuration metadata.

- `owner` (query): Administrator only: tenant ID. Omit to use the authenticated caller; not an all-tenant query.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      }
    }
  }
}
```

## DELETE /api/v2/backups/{id}

Delete local full archive. Account scoped. Removes local archive; remote copies are not removed.

- `id` (path): Opaque path value.
- `owner` (query): Administrator only: tenant ID. Omit to use the authenticated caller; not an all-tenant query.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/backups/{id}/download

Download .cgp archive. Account scoped authenticated binary ZIP download. Contains secrets. Use a private destination file.

- `id` (path): Opaque path value.
- `owner` (query): Administrator only: tenant ID. Omit to use the authenticated caller; not an all-tenant query.


Success response schema:

```json
{
  "application/octet-stream": {
    "schema": {
      "type": "string",
      "format": "binary"
    }
  }
}
```

## GET /api/v2/monitor/{id}

Read website monitoring. Domain ID; owner/admin only. Settings, latest 120 checks and 24-hour totals. Analytics tracking key is included.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v2/monitor/{id}

Replace monitoring configuration. Domain ID. Send complete configuration. Two failed probes open an outage. Interval 60–3600 seconds; retention 1–30 days. Telegram integration must belong to domain owner.

- `id` (path): Opaque path value.

Request schema: `Monitor` in the OpenAPI contract.

```json
{
  "enabled": true,
  "scheme": "https",
  "path": "/",
  "interval": 60,
  "telegram_id": "",
  "analytics": true,
  "clicks": true,
  "retention_days": 30
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/analytics/{id}

Read website analytics and heatmap. Domain ID; owner/admin. Data within configured retention, at most 30 days. daily, pages, heatmap, targets and referrers. IPs are site/day salted hashes, not raw addresses.

- `id` (path): Opaque path value.
- `path` (query): Page path for heatmap, defaults to most viewed page
- `viewport` (query): desktop (default) or mobile


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/domains/{id}/tls

Certificate and renewal status. Domain ID; owner/admin. installed, certificate, renewal_timer_active, last_renewal_run, validation and cdn.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/domains/{id}/zone

Download BIND DNS zone. Domain ID; owner/admin. Import at your provider. Does not change nameservers or enable CDN.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "text/plain": {
    "schema": {
      "type": "string"
    }
  }
}
```

## POST /api/v2/domains/{id}/cdn

Set trusted CDN mode. Domain ID; owner/admin. Cloudflare mode accepts visitor headers only from known provider networks.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "provider": "cloudflare"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v2/egress/{id}

Read application proxy policy. App ID; owner/admin. Change it by submitting an egress job.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "properties": {
        "proxy_id": {
          "type": "string"
        },
        "locked": {
          "type": "boolean"
        }
      }
    }
  }
}
```

## POST /api/v5/apps/{id}/console

Stream application command output. Owned application. Runs a container command for at most 25 seconds. Newline-delimited JSON output, bounded to 2 MiB. Disconnect cancels the command.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "command": "printf hello"
}
```

Success response schema:

```json
{
  "application/x-ndjson": {
    "schema": {
      "type": "string"
    }
  }
}
```

## POST /api/v5/network-range

Preview IP and CIDR ranges. Returns canonical first and last addresses or validation errors for comma/newline-separated inputs.

Request schema: `object` in the OpenAPI contract.

```json
{
  "value": "10.10.10.0/24"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true
      }
    }
  }
}
```

## POST /api/v5/apps/{id}/git

Workspace Git tools. Owned application; fixed status, history, diff or init operations. Full Git is available inside the IDE.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "operation": "status"
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v5/apps/{id}/services

Transfer service settings. Owned application. Returns SFTP and FTPS status, username, directory and IP restrictions.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/apps/{id}/services

Configure SFTP and FTPS. Owned application. FTPS requires explicit TLS. A new or rotated password is returned once. Existing sessions close when access is disabled or credentials rotate.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "sftp": true,
  "ftps": false,
  "rotate": false,
  "allowed_ips": [
    "203.0.113.0/24"
  ]
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v5/users/{id}/budget

Read account resource budget. Own account or administrator. Returns budget, allocated, remaining, and per-application enforcement.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/users/{id}/budget

Set account resource budget. Administrator only. Cannot set below existing allocations. Disk covers application workspaces, not mail, databases or container images.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "memory_mb": 2048,
  "cpu_millis": 2000,
  "disk_mb": 4096
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/apps/{id}/limits

Allocate application resources. Owned running application. Allocations must fit account budget. Workspace migration/growth briefly stops its app and IDE; disk shrinking is rejected.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "memory_mb": 512,
  "cpu_millis": 1000,
  "disk_mb": 1024
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/apps/{id}/backup-import

Upload a cgp archive. Owned application. begin supplies size and returns upload; chunk supplies upload, offset and base64 data (at most 196608 decoded bytes); finish validates ownership and registers the archive; cancel removes a partial upload. Limit 2 GiB and two active uploads. Restore is a separate v2 job.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "operation": "begin",
  "size": 1048576
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v5/protection/{id}

Website protection settings. Owned domain. Provider secret is omitted; secret_set indicates an existing secret.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/protection/{id}

Configure website protections. Owned domain. WAF off/detect/enforce; crawlers allow/block_ai/block_all; CAPTCHA off/local/turnstile/hcaptcha/recaptcha. Blank provider secret preserves current. CAPTCHA gates site pages and may affect APIs or webhooks. Rate controls cannot absorb upstream network saturation.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "requests_per_second": 20,
  "connections": 30,
  "waf": "detect",
  "crawlers": "allow",
  "captcha": "off",
  "site_key": "",
  "secret": "",
  "sitemap": true,
  "sitemap_paths": [
    "/"
  ]
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v5/protection/{id}/traffic

Recent traffic summary. Owned domain. Aggregates most recent 2000 logged requests, not a fixed time window. Unusual flags are heuristics, not proof of an attack.

- `id` (path): Opaque path value.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v5/mfa

Own MFA status. Includes count of remaining recovery codes.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/mfa/enroll

Begin authenticator enrollment. Browser session, CSRF and current password. Returns secret and otpauth URL; expires in ten minutes.

Request schema: `object` in the OpenAPI contract.

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/mfa/confirm

Confirm authenticator enrollment. Browser session and CSRF. Activates a valid six-digit code, revokes other sessions and returns one-time recovery codes.

Request schema: `object` in the OpenAPI contract.

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/mfa/disable

Disable own MFA. Browser session and CSRF. Requires current password and a valid authenticator/recovery code.

Request schema: `object` in the OpenAPI contract.

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## GET /api/v5/mail

Mail hosting status. Tenants see their domains and mailbox metadata; administrators see all. No password hashes are returned. See bundled mail guide for DNS, TLS and provider requirements.


Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/mail/server

Configure mail server. Administrator only. Set this VPS hostname, matching certificate domain ID, and public port state. Empty certificate_domain selects the development self-signed certificate.

Request schema: `object` in the OpenAPI contract.

```json
{
  "hostname": "mail.example.com",
  "certificate_domain": "DOMAIN_ID",
  "enabled": true
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/mail/domains/{id}

Enable domain mail. Owned domain or administrator. Enable generates DKIM keys; disable requires all its mailboxes to be disabled.

- `id` (path): Opaque path value.

Request schema: `object` in the OpenAPI contract.

```json
{
  "enabled": true
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## POST /api/v5/mail/mailboxes

Create or update mailbox. Owned mail-enabled domain. Omit id to create; include id to update. Blank password on update preserves current. Quota 64–102400 MiB, maximum 30 mailboxes per tenant. Disable preserves messages.

Request schema: `object` in the OpenAPI contract.

```json
{
  "domain_id": "DOMAIN_ID",
  "local": "hello",
  "password": "PRIVATE_PASSWORD",
  "quota_mb": 512,
  "enabled": true
}
```

Success response schema:

```json
{
  "application/json": {
    "schema": {
      "type": "object",
      "additionalProperties": true
    }
  }
}
```

## Resource creation examples

Add `owner: "TENANT_ID"` for an administrator creating on behalf of a tenant. Replace placeholder IDs and addresses.

### POST /api/resources/apps

```json
{
  "owner": "TENANT_ID",
  "name": "website",
  "runtime": "php",
  "mode": "web",
  "command": "",
  "env": {}
}
```

### POST /api/resources/domains

```json
{
  "owner": "TENANT_ID",
  "name": "example.com",
  "app_id": "APP_ID"
}
```

### POST /api/resources/databases

```json
{
  "owner": "TENANT_ID",
  "name": "website",
  "engine": "mysql",
  "allowed_ips": []
}
```

### POST /api/resources/dns

```json
{
  "owner": "TENANT_ID",
  "name": "www",
  "domain_id": "DOMAIN_ID",
  "type": "A",
  "value": "203.0.113.20",
  "ttl": 300
}
```

### POST /api/resources/schedules

```json
{
  "owner": "TENANT_ID",
  "name": "cleanup",
  "app_id": "APP_ID",
  "schedule": "0 3 * * *",
  "timezone": "UTC",
  "command": "python cleanup.py"
}
```

### POST /api/resources/backups

```json
{
  "owner": "TENANT_ID",
  "name": "snapshot",
  "app_id": "APP_ID"
}
```

### POST /api/resources/blocks

```json
{
  "owner": "TENANT_ID",
  "name": "203.0.113.55"
}
```

## Resource action payloads

All use `POST /api/resource/{id}/{action}`. Empty requests still send `{}`.

| Resource / action | JSON body | Result / behavior |
| --- | --- | --- |
| apps / start, stop, restart | `{}` | Start/stop/restart the owned container |
| apps / inspect, logs | `{}` | Captured inspect/log output |
| apps / terminal | `{"command":"id"}` | UID 1000 command, 25-second timeout |
| apps / files | `{}` | Workspace file listing, depth at most 3 |
| apps / read | `{"path":"index.html"}` | Text in `output`; workspace-relative path only |
| apps / write | `{"path":"index.html","content":"Hello"}` | Write bounded text inside workspace |
| databases / access | `{"allowed_ips":["203.0.113.10"]}` | Replace IP/CIDR database remote allowlist (MySQL IPv6 networks require individual addresses) |
| schedules / status | `{}` | Next run and bounded execution history |
| backups / restore | `{"confirm":"RESTORE"}` | Legacy workspace snapshot restore |
| domains / tls | `{"email":"admin@example.com","validation":"http","agree_tos":true}` | Legacy synchronous HTTP certificate request; prefer v2 job |

## Integration creation examples

Secrets here are placeholders. Store the real JSON privately. On edit, send id plus all intended nonsecret fields; blank secret fields keep prior values. New integrations require the type-specific fields shown.

### proxy

```json
{
  "type": "proxy",
  "name": "proxy-connection",
  "owner": "TENANT_ID",
  "url": "socks5h://proxy.example.com:1080"
}
```

### telegram

```json
{
  "type": "telegram",
  "name": "telegram-connection",
  "owner": "TENANT_ID",
  "token": "BOT_TOKEN",
  "chat_id": "CHAT_ID"
}
```

### s3

```json
{
  "type": "s3",
  "name": "s3-connection",
  "owner": "TENANT_ID",
  "endpoint": "https://s3.example.com",
  "bucket": "backups",
  "region": "us-east-1",
  "access_key": "ACCESS_KEY",
  "secret_key": "SECRET_KEY",
  "prefix": "cgpanel"
}
```

### ssh

```json
{
  "type": "ssh",
  "name": "ssh-connection",
  "owner": "TENANT_ID",
  "host": "backup.example.com",
  "port": 22,
  "username": "backup",
  "remote_dir": "/backups",
  "private_key": "OPENSSH_PRIVATE_KEY",
  "host_key": "VERIFIED_KEY_TYPE BASE64_KEY"
}
```

### cloudflare

```json
{
  "type": "cloudflare",
  "name": "cloudflare-connection",
  "owner": "TENANT_ID",
  "token": "SCOPED_DNS_TOKEN"
}
```

## Background job payloads

POST these flat objects to `/api/v2/jobs`, then poll the job list. Integration tests can send an actual Telegram message or upload a small test archive. Restore overwrites selected existing data. Certificate requests require actual agreement with the provider.

### runtime

```json
{
  "kind": "runtime",
  "target": "APP_ID",
  "version": "3.14",
  "command": ""
}
```

### ide

```json
{
  "kind": "ide",
  "target": "APP_ID",
  "enabled": true
}
```

### full_backup

```json
{
  "kind": "full_backup",
  "target": "APP_ID",
  "database_ids": [
    "DATABASE_ID"
  ],
  "destinations": [],
  "quiesce": true
}
```

### backup_deliver

```json
{
  "kind": "backup_deliver",
  "target": "BACKUP_ID",
  "owner": "TENANT_ID",
  "destinations": [
    "STORAGE_ID"
  ]
}
```

### restore_full

```json
{
  "kind": "restore_full",
  "target": "APP_ID",
  "backup_id": "BACKUP_ID",
  "confirm": "RESTORE"
}
```

### integration_test

```json
{
  "kind": "integration_test",
  "target": "INTEGRATION_ID",
  "owner": "TENANT_ID"
}
```

### tls

```json
{
  "kind": "tls",
  "target": "DOMAIN_ID",
  "email": "admin@example.com",
  "validation": "http",
  "agree_tos": true,
  "staging": true
}
```

### panel_tls

```json
{
  "kind": "panel_tls",
  "target": "ADMIN_ID",
  "email": "admin@example.com",
  "agree_tos": true,
  "staging": true
}
```

### seo

```json
{
  "kind": "seo",
  "target": "DOMAIN_ID",
  "scheme": "https"
}
```

### egress

```json
{
  "kind": "egress",
  "target": "APP_ID",
  "proxy_id": "PROXY_ID",
  "locked": true
}
```

## Public telemetry

The public website tracker is served from `/telemetry/tracker.js`; ingestion is `POST /telemetry/collect/{domain_id}`. These are website instrumentation routes, not administrator management endpoints. The per-domain Nginx `/__cgpanel/` mapping exposes the same-origin tracker/collector. Use the generated setup snippet in Website analytics rather than an administrator token. Ingestion requires the site tracking key, matching Origin and enabled configuration; it enforces separate size/rate limits and honors Do Not Track.
