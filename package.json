{
  "name": "cgpanel-ui",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i web/input.css -o web/style.css --minify",
    "check:ui": "node --check web/app.js && node --check web/icons.js && node --check web/features.js && node --check web/tracker.js && node --check web/documentation.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "prettier": "3.9.9",
    "tailwindcss": "^4.3.0"
  }
}
